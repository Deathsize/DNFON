<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="org.dnfon.mapper.MemberMapper">
  	<!-- 아이디체크 -->
  	<select id="id_check" resultType="int">
		<![CDATA[
			select count(*) from member where id = #{userid}
		]]>
	</select>
	<!-- 닉네임 체크 -->
	<select id="nick_check" resultType="int">
		<![CDATA[
			select count(*) from member where nick = #{usernick}
		]]>
	</select>
	<!-- 회원가입 -->
	<insert id="insert">
		insert into member (id, pw, name, nick, email)
		values (#{id}, #{pw}, #{name}, #{nick}, #{email})
	</insert>
  </mapper>